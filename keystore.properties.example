# Sao chép file này thành keystore.properties (KHÔNG commit file này lên git!)
# Copy this file to keystore.properties (NEVER commit keystore.properties to git!)

storeFile=../my-release-key.jks
storePassword=your_keystore_password
keyAlias=your_key_alias
keyPassword=your_key_password
